// Includes
#include "IntroState.h"

namespace Game
{
	IntroState::IntroState()
	{
	}

	IntroState::~IntroState()
	{
	}

	void IntroState::Update(float t)
	{
		// Game::CameraManager::Instance().GetCamera()->setPosition(Vector3((n->getPosition().x),(n->getPosition().y) + 300,(n->getPosition().z)+ 600));
	}

	void IntroState::Init()
	{

	}

}